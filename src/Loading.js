import React from "react";

const Loading = props => {
    return (
        <div className="list-books-loader">
            <div className="loader" />
        </div>
    );
};

export default Loading;
